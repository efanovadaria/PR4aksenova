<Page x:Class="PR4aksenova.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Page1">

    <ScrollViewer>
        <Grid Margin="20" Height="414" Width="344">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Картинка формулы -->
            <Image Grid.Row="0" Height="120" Margin="0,0,0,20"
                   Source="C:\Users\Huawei\source\repos\PR4aksenova\PR4aksenova\Images\formula.png"/>

            <!-- X -->
            <Label Grid.Row="1" Content="Введите X:"/>
            <TextBox x:Name="txtX" Grid.Row="2" Margin="0,0,0,10"/>

            <!-- Y -->
            <Label Grid.Row="3" Content="Введите Y:"/>
            <TextBox x:Name="txtY" Grid.Row="4" Margin="0,0,0,10"/>

            <!-- Z -->
            <Label Grid.Row="6" Content="Введите Z:" Margin="0,0,0,42"/>
            <TextBox x:Name="txtZ" Grid.Row="6" Margin="0,31,0,19"/>

            <!-- Результат -->
            <Label Grid.Row="6" Content="Результат:" Margin="0,60,0,-21"/>
            <TextBox x:Name="txtResult" Grid.Row="6"
                     IsReadOnly="True" Margin="0,89,0,-39"/>

            <!-- Кнопки -->
            <StackPanel Grid.Row="6" Orientation="Horizontal"
                        HorizontalAlignment="Center" Margin="0,121,0,-88">

                <Button Content="Вычислить"
                        Width="92"
                        Margin="5"
                        Click="BtnCalculate_Click" Height="22"/>

                <Button Content="Очистить"
                        Width="81"
                        Margin="5"
                        Click="BtnClear_Click" Height="19"/>

                <Button Content="Далее"
                        Width="82"
                        Margin="5"
                        Click="BtnNext_Click" Height="17"/>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Page>