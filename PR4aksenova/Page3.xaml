<Page x:Class="PR4aksenova.Page3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration">
    <ScrollViewer>
        <Grid Margin="15">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="200"/>
                <RowDefinition Height="300"/>
            </Grid.RowDefinitions>

            <!-- Формула -->
            <Image Grid.Row="0"
                   Height="120"
                   Margin="0,0,0,15"
                   Source="C:\Users\Huawei\source\repos\PR4aksenova\PR4aksenova\Images\formula3.png"/>

            <!-- Ввод данных -->
            <StackPanel Grid.Row="1" Orientation="Vertical">

                <Label Content="x0:"/>
                <TextBox x:Name="txtX0"/>

                <Label Content="xk:"/>
                <TextBox x:Name="txtXk"/>

                <Label Content="dx:"/>
                <TextBox x:Name="txtDx"/>

                <Label Content="b:"/>
                <TextBox x:Name="txtB"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,10">

                <Button Content="Вычислить"
                        Width="100"
                        Margin="5"
                        Click="BtnCalculate_Click"/>

                <Button Content="Очистить"
                        Width="100"
                        Margin="5"
                        Click="BtnClear_Click"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
                <Button Content="Выход" Width="100" Margin="5" Click="BtnExit_Click"/>
            </StackPanel>

            <!-- Многострочный вывод -->
            <TextBox x:Name="txtOutput"
                     Grid.Row="5"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"/>

            <!-- График -->
            <wfi:WindowsFormsHost x:Name="ChartHost" Grid.Row="6" Margin="5"/>
        </Grid>
    </ScrollViewer>
</Page>